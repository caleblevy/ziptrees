// Initial toy implementation of zip tree in c

#include <stdio.h>

typedef struct tree_node Tree;
struct node {
    Tree * left, * right;
    int key;
    unsigned int rank;
};


Tree * search(int k, Tree * T) {
    
}

Tree * insert_zip(int k, Tree * root) {
    Tree * new
}


void main() {
    Tree * root;
    int i;
    printf("size = %d\n", 1);
}
